/** @type {import('tailwindcss').Config} */
module.exports = {
  // for every sub-folder and file
  content: ["./src/**/*.{html, js, jsx, ts, tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
